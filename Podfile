# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS组件化' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for iOS组件化

  target 'iOS组件化Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS组件化UITests' do
    inherit! :search_paths
    # Pods for testing
  end
pod 'CPFNetWorking', :path => 'Lib/CPFNetWorking'
pod 'CPFLogin', :path => 'Lib/CPFLogin'
end
